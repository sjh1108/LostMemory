spring:
  jpa:
    hibernate:
      ddl-auto: validate    # 운영은 검증만 (스키마 변경 금지)
    show-sql: false

logging:
  level:
    root: info
    com.yourpackage: info
  file:
    name: /var/log/app/application.log