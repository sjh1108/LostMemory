spring:
  config:
    import: optional:file:.env.dev[.properties]
  jpa:
    hibernate:
      ddl-auto: update      # 로컬은 자동 스키마 갱신
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    com.lostmemory: debug