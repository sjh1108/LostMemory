spring:
  config:
    import: optional:file:.env.prod[.properties]

management:
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    root: info
    com.lostmemory.aiserver: info
