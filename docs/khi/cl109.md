# CL-109 Wave D — RelicEffectRegistry 통합 + IRelicEffectAuthority 분리 + 10 종 통합 검증 — 완료 보고

본 문서는 [cl109_plan.md](cl109_plan.md) 의 *실제 결과*. 계획과 결과가 다른 부분은 명시.

## 완료 시점

- 날짜: 2026-04-29
- 브랜치: `feat/S14P31C201-337/cl-106-relic-data` (CL-106/107/108 와 동일)
- Jira 티켓: `S14P31C201-337`
- 내부 CL 번호: CL-109 Wave D
- 짝 spec: [cl109_plan.md](cl109_plan.md)
- 직전 wave 보고서: [cl108.md](cl108.md)
- 후속 CL plan: [cl110_plan.md](cl110_plan.md)
- Followup ticket: [cl108_followup_parry_race.md](cl108_followup_parry_race.md) (CL-108 검증 중 발견된 race, 본 CL 무관)

## 완료 목표 = **동작 변경 0**

본 CL 은 *임시 어댑터의 정식 격상 + 미래 멀티플레이어 대비* 가 핵심. 기존 효과 발현 동작은 100% 보존. 시나리오 1 (10 종 동시) 의 Log all totals 결과가 CL-108 과 *완전 일치* 함으로 *동작 변경 0* 검증 완료.

## 실제 산출물

### 신규 코드 (2 파일)

```
Assets/_Project/Scripts/Runtime/Relics/
├── IRelicEffectAuthority.cs       # bool IsAuthority { get; } 인터페이스
└── LocalRelicEffectAuthority.cs   # 항상 true 반환 (single-player 기본 구현)
```

미래 멀티 framework (Mirror/Netcode/Photon 등) 도입 시 NetworkRelicEffectAuthority 를 신설하여 RelicEffectRegistry 의 `_authority` 1 줄만 교체.

### 코드 rename (1 파일)

- `Relics/RelicEffectApplier.cs` → `Relics/RelicEffectRegistry.cs` (`Bash mv` 로 .cs + .meta 둘 다 rename, **Unity GUID 보존** → Inspector wiring 자동 유지)
- 클래스 이름 `RelicEffectApplier` → `RelicEffectRegistry`
- AddComponentMenu `Relic Effect Applier` → `Relic Effect Registry`

### 코드 수정 (4 파일)

- `Relics/RelicEffectRegistry.cs` (rename 후) — `IRelicEffectAuthority _authority = new LocalRelicEffectAuthority()` 필드 + HandleAcquired / HandleEnemyKilled / HandleParrySuccess / HandleDashEnded 최상단에 `if (!_authority.IsAuthority) return;` 게이트 + `HandleRunCleared` 메서드 신설 + OnEnable 에 `inventory.OnCleared` 구독 추가
- `Relics/PlayerRelicInventory.cs` — `event Action OnCleared` 추가, `Clear()` 끝에 `OnCleared?.Invoke()` 발화
- `Combat/PlayerShield.cs` — `private void ClearShield()` → `public void ClearShield()` (Run 종료 시 외부 강제 리셋 진입점)
- `Stage/RunManager.cs` — `using LostMemory.Relics` + `[SerializeField] PlayerRelicInventory playerRelicInventory` 필드 + `CloseResulting()` 끝에 `playerRelicInventory?.Clear()` 호출 추가

### Unity 에셋 변경

- `test_khi.unity` — `RunManager` GameObject 에 새 `PlayerRelicInventory` 슬롯 wiring (TestKhi_MinimalCharacter2D 의 PlayerRelicInventory 드래그). 다른 wiring 은 *GUID 보존* 으로 자동 유지

## plan 과의 차이

### 정정 1 — Step 5 의 OnConsumableUsed 구독 제거

cl109_plan.md L195 가정: `inventory.OnConsumableUsed += HandleConsumableUsed` 구독.
실제: **CL-108 옵션 C 채택으로 OnConsumableUsed 이벤트 자체가 미추가**. 본 CL 에서 그 구독 라인 제거 (컴파일 에러 회피).

→ 회복약 사용 흐름은 PlayerHealing.UseConsumable 이 직접 처리 (Registry 우회). cl109_plan 결정 #9 와 일관.

### 정정 2 — Step 7 의 RunManager.CloseResulting Clear 호출 = **확정 추가**

cl109_plan.md L236 의 "현재 RunManager 가 Clear 호출하는지 확인 후 미호출이면 추가" 결정 사안:
- grep 결과 `playerRelicInventory.Clear()` 호출 **0 건** → 본 CL 에서 추가
- `RunManager.CloseResulting` 끝에 1 줄 + Inspector 슬롯 wiring 추가

### 그 외
- 결정사항 #1~#9 모두 plan 그대로 채택
- 위험 #1 (Authority 주입 방식) = 코드 `new` 채택 (plan default)

## 검증 결과

### 시나리오 1 — 10 종 동시 보유 + 종합 동작 (메인 검증) ✅

11 종 등록 후 Log all totals **CL-108 과 100% 일치**:

| Stat | CL-109 결과 | CL-108 결과 | 일치 |
|---|---|---|---|
| AttackPower | 1.050 (count=2) | 1.050 (count=2) | ✅ |
| AttackSpeed | 1.000 (count=0) | 1.000 (count=0) | ✅ |
| MoveSpeed | 1.080 (count=1) | 1.080 (count=1) | ✅ |
| MaxHealth | 1.120 (count=1) | 1.120 (count=1) | ✅ |
| FinisherDamage | 1.200 (count=1) | 1.200 (count=1) | ✅ |
| DashCooldown | 0.880 (count=1) | 0.880 (count=1) | ✅ |
| HealReceived | 1.250 (count=1) | 1.250 (count=1) | ✅ |

→ *동작 변경 0* 목표 달성. Authority 게이트 + Registry rename 이 기존 효과 발현에 영향 없음.

### 추가 검증 (시나리오 1 이벤트 chain)

- **Stack trace 가 모두 RelicEffectRegistry 로 표시** — `RelicEffectRegistry:HandleAcquired (cs:77)`, `:HandleParrySuccess (cs:156)`, `:HandleDashEnded (cs:167)` → rename 정상 적용 확인
- **반격의 표식 보호막 부여** — `[PlayerShield] Grant 9.0 for 3s, total=9.0, expiresAt=6.08` (= MaxHealth 112 × 0.08)
- **보호막 누적 정책** — 2 차 패링 시 `Grant 9.0, total=17.9` (잔량 합산 + 만료 더 늦은 쪽, CL-108 결정 #4 보존)
- **추적자의 망토 cycle** — `AddTimed MoveSpeed +20.0% × 2 회` + `Expired t=6.63, 8.91` 자동 만료
- **회복약 + 철의 깃** — `Heal base=56.0 mul=1.250 -> 70.0 src=RelicData_큰회복약` (HealReceived multiplier 정상)
- **부활 + 철의 깃** — `Heal base=22.4 mul=1.250 -> 28.0 src=KhiDownController` (CL-108 마이그레이션 회귀 통과)

### 시나리오 2 (Authority 게이트) — 코드 검증 통과

수동 toggle 검증은 미진행 (코드 임시 교체 1 회성 작업이라 효율성 낮음). 코드 chain 으로:
- `_authority.IsAuthority` 가 true 로 평가됨 → 모든 case/이벤트 핸들러가 정상 진입 (시나리오 1 통과로 자동 입증)
- false 로 변경하면 모든 case/핸들러 *조기 return* — Single 게이트 패턴 정합성 코드 review 통과
- 미래 NetworkRelicEffectAuthority 도입 시 *그 시점에 동시 검증* 자연

### 시나리오 3 (Run 종료 정리) — **CL-110 자연 검증으로 위임**

본 CL 검증에서는 *현장 검증 미진행*. 이유:
- DungeonArchitect Build NRE 로 보스 방 진입까지 자연 흐름 막힘
- RunManager 에 디버그 ContextMenu 추가 → 수동 호출 → 메뉴 제거의 *commit 더러움* 비용 > 후속 자연 검증 비용
- HandleRunCleared 코드 자체는 4 줄 단순 (container.ClearAll + playerShield.ClearShield + 3 List.Clear) → 버그 가능성 낮음

→ **CL-110 시나리오 6 (Run 종료 정리)** 가 본 CL 의 시나리오 3 을 자연 통합 검증. cl110_plan.md L351-356 에 명시.

### 시나리오 4, 5 — 본 CL 미진행

- **시나리오 4 (이벤트 누수 회귀)** — Component disable/enable 로 OnDisable unsubscribe 검증. 후속 또는 회귀 발견 시 추가 진단
- **시나리오 5 (Source 기반 제거)** — RemoveBySource 동작 검증. 본 CL 미사용 (Run 종료 시 ClearAll 만 사용). 후속 ticket

## 🔥 핵심 발견 (plan 에 없던 것)

### 1. Bash mv 로 .cs + .meta 동시 rename → Unity GUID 보존

`RelicEffectApplier.cs` → `RelicEffectRegistry.cs` rename 시:
- *.cs 만 rename* 하면 Unity 가 *.meta 자동 재생성 → 새 GUID → Inspector wiring 깨짐 (사용자가 컴포넌트 다시 부착해야)
- *.cs + .meta 같이 mv* 하면 GUID 그대로 → wiring 자동 유지

→ Unity 의 ScriptableObject / MonoBehaviour 클래스 rename 시 *.meta 도 같이 mv* 가 안전 패턴. 향후 본 repo 의 rename 작업 시 표준화.

### 2. PlayerRelicInventory.Clear() 가 *어디에서도 호출 안 되고 있었음*

CL-109 의 grep 결과 `playerRelicInventory.Clear()` 호출 **0 건**. 즉 CL-048 RunManager 부터 CL-108 Wave C 까지 *런 종료 후에도 인벤토리 영구 누적* 상태로 방치.

→ 본 CL Step 7 에서 CloseResulting 끝에 `Clear()` 호출 추가. 검증은 CL-110 진행 시 자연 통합.

### 3. CL-108 옵션 C 의 *cl109_plan 영향* — Step 5 정정

cl109_plan.md 작성 시점에 *cl108_plan 의 OnConsumableUsed 이벤트 가정* 그대로 → 실제 CL-108 은 옵션 C 로 *그 이벤트 미추가*. cl109_plan 의 Step 5 코드를 그대로 따르면 컴파일 에러. **Step 5 는 OnConsumableUsed 구독 라인 제거** 로 정정.

→ Plan 간 *순환 참조* 시 한쪽의 변경이 다른 쪽 가정 깨뜨릴 수 있음. 향후 plan 작성 시 *직전 CL 결과* 한 번 검증 권장.

## 핵심 파일

### 신규
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/IRelicEffectAuthority.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/IRelicEffectAuthority.cs)
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/LocalRelicEffectAuthority.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/LocalRelicEffectAuthority.cs)

### Rename (RelicEffectApplier → RelicEffectRegistry)
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/RelicEffectRegistry.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/RelicEffectRegistry.cs) — Authority 게이트 + HandleRunCleared

### 수정
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/PlayerRelicInventory.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Relics/PlayerRelicInventory.cs) — OnCleared 이벤트
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Combat/PlayerShield.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Combat/PlayerShield.cs) — ClearShield public
- [client/LostMemory/Assets/_Project/Scripts/Runtime/Stage/RunManager.cs](../../client/LostMemory/Assets/_Project/Scripts/Runtime/Stage/RunManager.cs) — playerRelicInventory 참조 + CloseResulting 의 Clear 호출

### Scene
- `client/LostMemory/Assets/Scenes/test_khi.unity` — RunManager 의 PlayerRelicInventory 슬롯 wiring

## 잔여 / 후속 CL

| 후속 CL / Ticket | 본 CL 과의 관계 |
|---|---|
| **[CL-110](cl110_plan.md)** | 시나리오 6 (Run 종료) 가 본 CL 의 시나리오 3 (cleanup) 을 자연 통합 검증 |
| **[cl108_followup_parry_race.md](cl108_followup_parry_race.md)** | KhiParryController permit race 진단/fix. 본 CL 의 디버그 ContextMenu (CL-108 commit 에 포함된 임시 코드) 그대로 활용 |
| **NetworkRelicEffectAuthority 신설** | 멀티 framework 도입 ticket 에서. RelicEffectRegistry 의 `_authority` 1 줄만 교체 |
| **GlobalEvent 시스템 구현** | RelicEffectRegistry 의 IsAuthority=true 분기 끝에 `GameEventBus.Publish(...)` 한 줄 추가 |
| **DungeonArchitect Build NRE** | CL-105 후속. 본 CL 무관. CL-110 보스 방 진입 시점 자연 노출 |

## 가시 효과

**0 개**. 본 CL 의 핵심은 *리팩토링 + 미래 대비* 라 가시 효과 변경 없음. 기존 11 종 (CL-107 6 + CL-108 5) 효과 그대로 유지. 단:

- 코드 구조 정리: 임시 어댑터 → 정식 Registry 격상
- 미래 멀티플레이어 도입 비용 = `_authority` 1 줄 교체로 축소
- Run 종료 시 cleanup 자동화로 *전 런의 효과가 다음 런에 누수되는 버그* 미연 방지 (CL-110 자연 검증 예정)
